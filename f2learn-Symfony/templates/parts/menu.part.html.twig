<div class="page-wraper">
    <div id="loading-icon-bx"></div>
    <!-- Header Top ==== -->
    <header class="header rs-nav">
        <div class="top-bar">
            <div class="container">
                <div class="row d-flex justify-content-between">
                    <div class="topbar-left">
                        <ul>
                            <li><a href="{{ path('faqs') }}"><i class="fa fa-question-circle"></i>Ask a Question</a></li>
                            <li><a href="mailto:f2learn.education@gmail.com"><i class="fa fa-envelope-o"></i>f2learn.education@gmail.com</a></li>
                        </ul>
                    </div>
                    <div class="topbar-right">
                        <ul>
                            {#
                            <li><a href="?language=en_GB"><img src="{{ asset('assets/images/flag/united-kingdom.svg') }}" width="18px" alt="en-GB" /> English</a></li>
                            <li><a href="?language=es_ES"><img src="{{ asset('/assets/images/flag/spain.svg') }}" width="18px" alt="es-ES" /> Spanish</a></li>
                            #}
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <li><a href="{{ path('app_logout') }}"><i class="fa fa-sign-out" aria-hidden="true"></i> <b>{{ app.user.userIdentifier }}</b> - Exit</a></li>
                            {% else %}
                                <li><a href="{{ path('login') }}"><i class="fa fa-user" aria-hidden="true"></i> Login</a></li>
                                <li><a href="{{ path('register') }}"><i class="fa fa-user-plus" aria-hidden="true"></i> Register</a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <div class="sticky-header navbar-expand-lg">
            <div class="menu-bar clearfix">
                <div class="container clearfix">
                    <!-- Header Logo ==== -->
                    <div class="menu-logo {# <?= f2learn\app\utils\Utils::esOpcionMenuActiva('index') ? 'active' : ''?> #} ">
                        <a href="{{ path('index') }}"><img src="{{ asset('assets/images/logo.png') }}" alt=""></a>
                    </div>
                    <!-- Mobile Nav Button ==== -->
                    <button class="navbar-toggler collapsed menuicon justify-content-end" type="button" data-toggle="collapse" data-target="#menuDropdown" aria-controls="menuDropdown" aria-expanded="false" aria-label="Toggle navigation">
                        <span></span>
                        <span></span>
                        <span></span>
                    </button>
                    <!-- Author Nav ==== -->
                    <div class="secondary-menu">
                        <div class="secondary-inner">
                            <ul>
                                <li><a href="https://es-es.facebook.com/" target="_blank" class="btn-link"><i class="fa fa-facebook"></i></a></li>
                                <li><a href="https://www.instagram.com/" target="_blank" class="btn-link"><i class="fa fa-instagram"></i></a></li>
                                <li><a href="https://es.linkedin.com/" target="_blank" class="btn-link"><i class="fa fa-linkedin"></i></a></li>
                                <!-- Search Button ====
                                <li class="search-btn"><button id="quik-search-btn" type="button" class="btn-link"><i class="fa fa-search"></i></button></li>-->
                            </ul>
                        </div>
                    </div>
                    <!-- Search Box ====
                    <div class="nav-search-bar">
                        <form action="#">
                            <input name="search" value="" type="text" class="form-control" placeholder="Type to search">
                            <span><i class="ti-search"></i></span>
                        </form>
                        <span id="search-remove"><i class="ti-close"></i></span>
                    </div>-->
                    <!-- Navigation Menu ==== -->
                    <div class="menu-links navbar-collapse collapse justify-content-start" id="menuDropdown">
                        <div class="menu-logo">
                            <a href="{{ path('index') }}"><img src="{{ asset('assets/images/logo.png') }}" alt=""></a>
                        </div>
                        <ul class="nav navbar-nav">
                            <li class="">
                                <a href="{{ path('index') }}">Home</a></li>
                            <li class="{% if ((path('about-us') in app.request.pathInfo) or (path('contact') in app.request.pathInfo) or (path('faqs') in app.request.pathInfo) or (path('portfolio') in app.request.pathInfo)) %}active{% endif %}"><a href="javascript:;">About  <i class="fa fa-chevron-down"></i></a>
                                <ul class="sub-menu">
                                    <li class="{% if (path('about-us') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('about-us') in app.request.pathInfo) %}#{% else %}{{ path('about-us') }}{% endif %}">About Us</a></li>
                                    <li class="{% if (path('contact') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('contact') in app.request.pathInfo) %}#{% else %}{{ path('contact') }}{% endif %}">Contact Us</a></li>
                                    <li class="{% if (path('faqs') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('faqs') in app.request.pathInfo) %}#{% else %}{{ path('faqs') }}{% endif %}">FAQ's</a></li>
                                    <li class="{% if (path('portfolio') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('portfolio') in app.request.pathInfo) %}#{% else %}{{ path('portfolio') }}{% endif %}">Portfolio</a></li>
                                </ul>
                            </li>
                            <li class="add-mega-menu {% if ((path('our-courses') in app.request.pathInfo) or (path('membership') in app.request.pathInfo)) %}active{% endif %}"><a href="javascript:;">Courses <i class="fa fa-chevron-down"></i></a>
                                <ul class="sub-menu">
                                    <li class="{% if (path('our-courses') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('our-courses') in app.request.pathInfo) %}#{% else %}{{ path('our-courses') }}{% endif %}">Our Courses</a></li>
                                    <li class="{% if (path('membership') in app.request.pathInfo) %}active{% endif %}">
                                        <a href="{% if (path('membership') in app.request.pathInfo) %}#{% else %}{{ path('membership') }}{% endif %}">Membership</a></li>
                                </ul>
                            </li>
                            <li class="{% if (path('blog') in app.request.pathInfo) %}active{% endif %}">
                                <a href="{% if (path('blog') in app.request.pathInfo) %}#{% else %}{{ path('blog') }}{% endif %}">Blog</a></li>
                            <li><a href="{{ path('articles') }}">Articles</a></li>
                            <!-- Back Office -->
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <li class="{% if ('profile' in app.request.pathInfo) %}active{% endif %}">
                                    <a href="{% if ('profile' in app.request.pathInfo) %}#{% else %}{{ path('profile', { 'id': app.user.id }) }}{% endif %}">Profile</a></li>
                                {% if ('ADMIN' in app.user.roles[0]) %}
                                    <li class="nav-dashboard"><a href="javascript:;">Dashboard <i class="fa fa-chevron-down"></i></a>
                                        <ul class="sub-menu">
                                            <li><a href="{{ path('add-course') }}">Courses</a></li>
                                            <li><a href="{{ path('gallery') }}">Gallery Images</a></li>
                                            <li><a href="{{ path('blog-posts') }}">Blog Posts</a></li>
                                            <li><a href="{{ path('partners') }}">Partners</a></li>
                                            <li><a href="{{ path('users') }}">Users</a></li>
                                        </ul>
                                    </li>
                                {% endif %}
                            {% endif %}
                            <!-- END Back Office -->
                        </ul>
                        <div class="nav-social-link">
                            <a href="javascript:;"><i class="fa fa-facebook"></i></a>
                            <a href="javascript:;"><i class="fa fa-instagram"></i></a>
                            <a href="javascript:;"><i class="fa fa-linkedin"></i></a>
                        </div>
                    </div>
                    <!-- Navigation Menu END ==== -->
                </div>
            </div>
        </div>
    </header>
    <!-- header END ==== -->
    <!-- Inner Content Box ==== -->
    <div class="page-content">
        <!-- Page Heading Box ==== -->
        <div class="page-banner ovbl-dark" style="background-image:url({{ asset('assets/images/banner/banner2.jpg') }});">
            <div class="container">
                <div class="page-banner-entry">
                    <h1 class="text-white">
                        {% if ('faqs' in app.request.pathInfo) %}
                            FAQ's
                        {% else %}
                            {% if ('course' in app.request.pathInfo) %}
                                Our Courses
                            {% else %}
                                {% if ('portfolio' in app.request.pathInfo) %}
                                    Portfolio
                                {% else %}
                                    {% if ('-' in app.request.pathInfo or '/' in app.request.pathInfo) %}
                                        {{ app.request.pathInfo|slice(1)|replace({'-': ' ', '/': ' '})|capitalize }}
                                    {% else %}
                                        {{ app.request.pathInfo|slice(1)|capitalize }}
                                    {% endif %}
                                {% endif %}
                            {% endif %}
                        {% endif %}
                    </h1>
                </div>
            </div>
        </div>
        <div class="breadcrumb-row">
            <div class="container">
                <ul class="list-inline">
                    <li><a href="{{ path('index') }}">Home</a></li>
                    <li>
                        <b>
                            {% if ('faqs' in app.request.pathInfo) %}
                                FAQ's
                            {% else %}
                                {% if ('course' in app.request.pathInfo) %}
                                    Our Courses
                                {% else %}
                                    {% if ('portfolio' in app.request.pathInfo) %}
                                        Portfolio
                                    {% else %}
                                        {% if ('-' in app.request.pathInfo or '/' in app.request.pathInfo) %}
                                            {{ app.request.pathInfo|slice(1)|replace({'-': ' ', '/': ' '})|capitalize }}
                                        {% else %}
                                            {{ app.request.pathInfo|slice(1)|capitalize }}
                                        {% endif %}
                                    {% endif %}
                                {% endif %}
                            {% endif %}
                        </b>
                    </li>
                </ul>
            </div>
        </div>
        <!-- Page Heading Box END ==== -->